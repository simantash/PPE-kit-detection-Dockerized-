## 🐳 Docker Containerization (by Simanta)

This project is fully containerized using Docker. No need to install Python or dependencies manually.

### 🔧 Build the Docker image:
```bash
docker build -t ppe-detector .
